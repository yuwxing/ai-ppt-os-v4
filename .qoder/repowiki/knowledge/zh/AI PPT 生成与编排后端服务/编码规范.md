- 所有 AI 智能体类均继承自 `BaseAgent`，通过定义 `system_prompt` 和调用 `chat` 或 `json_output` 方法来实现标准化的 LLM 交互逻辑。
- API 路由模块统一使用 `APIRouter` 并设置 `prefix` 和 `tags`，在 `main.py` 中集中注册以维持入口文件的简洁性。
- 数据库操作与外部 API 调用均采用 `async/await` 模式，确保在高并发场景下主线程的非阻塞运行。