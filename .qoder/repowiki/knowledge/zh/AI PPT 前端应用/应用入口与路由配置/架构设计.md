- 入口层：`main.jsx` 作为应用启动点，使用 `BrowserRouter` 包裹根组件并挂载至 DOM。
- 路由层：`App.jsx` 定义顶层布局（Header + Main），并通过 `react-router-dom` 的 `Routes` 组件分发至各业务页面（如 HomePage, DashboardPage 等）。
- 样式层：`index.css` 引入 Tailwind CSS 基础指令，并定义全局字体及自定义工具类（如渐变背景、玻璃拟态效果）。