- 核心构建工具采用 Vite，通过 `vite.config.js` 配置 React 插件、开发服务器端口（3000）及 API 代理（指向 localhost:8001）。
- 样式处理基于 PostCSS 管线，集成 Tailwind CSS 进行原子化样式生成，并通过 `autoprefixer` 确保浏览器兼容性。
- 部署架构采用多阶段 Docker 构建：第一阶段使用 Node.js 22 Alpine 镜像编译产物，第二阶段将静态资源注入 Nginx Alpine 镜像提供生产服务。