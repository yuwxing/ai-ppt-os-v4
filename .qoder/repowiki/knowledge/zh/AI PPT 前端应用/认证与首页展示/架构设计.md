- AuthPage.jsx：通过 `client` 调用 `/users/login` 或 `/users/register`，成功后将 `access_token` 存入 `localStorage` 并跳转至 `/dashboard`。
- HomePage.jsx：纯静态展示组件，包含 Hero、特性、定价等区块，使用 `react-router-dom` 的 `Link` 进行导航。
- DashboardPage.jsx：在 `useEffect` 中校验 `localStorage` 中的 token，调用 `/users/me` 获取用户信息，失败则重定向回 `/auth`。