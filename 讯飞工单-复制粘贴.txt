==================================================
讯飞工单内容（复制下面虚线之间的文字提交）
==================================================

问题类型：智能PPT生成(新版) - 服务授权绑定问题

应用信息：
- AppId: 7c8babc9
- 应用名称：智能PPT生成

问题描述：
调用智能PPT生成(新版)所有接口均返回 HTTP 405 "Invalid AppId"。
涉及接口包括：
- POST https://zwapi.xfyun.cn/api/ppt/v2/template/list
- POST https://zwapi.xfyun.cn/api/ppt/v2/createOutline
- POST https://zwapi.xfyun.cn/api/ppt/v2/createPptByOutline
旧版接口 POST https://zwapi.xfyun.cn/api/aippt/create 同样返回 Invalid AppId。

已做的排查：
1. 签名算法 ApiAuthAlgorithm 已与官方文档(https://xfyun.cn/doc/spark/PPTv2.html)逐字节核对，MD5+HMAC-SHA1+base64实现完全一致。
2. 故意使用错误签名测试，返回的仍是"Invalid AppId"而非"鉴权错误(20007)"，证明问题出在 AppId 授权校验阶段，不是签名问题。
3. 完全不带鉴权头测试，返回"Invalid Param"，证明请求头格式正确。
4. 检查控制台 → 我的应用 → AppId 7c8babc9 详情页，"服务信息"中仅有"Spark X"（星火认知大模型）一项，未看到"智能PPT生成"或"智能PPT生成(新版)"服务。

请求：
请协助将"智能PPT生成(新版)"服务授权绑定到 AppId 7c8babc9，或告知正确的开通/绑定操作流程。
该应用当前只有星火大模型服务，缺少PPT生成服务授权，导致所有PPT接口无法调用。

==================================================
